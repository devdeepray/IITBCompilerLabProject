int add(int val1, int val2[3])
{
	float sum;
	int i;
	sum = val1;
	for(i=0; i < 3; i=i+1)
	{
		if(sum == 0)
		{
			sum = (sum = sum + val[i]);
		}
		else
		{
			sum = val[i+val[i]];	
		}
	
	}

	i  = sum || i;
	return sum;
}
